{
  "name": "user-management-app",
  "version": "1.0.0",
  "description": "User Management Dashboard with CRUD, Notifications, and Analytics",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "migrate": "mysql -u root -p < backend/sql/schema.sql",
    "seed": "mysql -u root -p < backend/sql/schema.sql"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.4",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}